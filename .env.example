# Server
PORT=8080
APP_ENV=development

# Database (docker)
DATABASE_URL=postgres://clinithink:password@localhost:5432/clinithink?sslmode=disable

# Redis (docker)
REDIS_URL=redis://localhost:6379

# JWT
JWT_SECRET=random-string-minimal-32-karakter
JWT_EXPIRY=24h

# Google Cloud TTS
GCP_TTS_API_KEY=
